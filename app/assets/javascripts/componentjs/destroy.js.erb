$(document).ready(function(){
	
	$('.destroy').on('click', function(){
		if(confirm("Are you sur ?")){
			$.ajax({
				url: '/emails' + this.parentElement.id,
				type: 'DELETE',
				success: function(r){
	  			}
	  		})
	   	}
 	 })
});